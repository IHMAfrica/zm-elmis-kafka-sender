apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "elmis-kafka-sender-chart.fullname" . }}-test-connection"
  labels:
    {{- include "elmis-kafka-sender-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "elmis-kafka-sender-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
