apiVersion: v1
kind: Service
metadata:
  name: {{ include "elmis-kafka-sender-chart.fullname" . }}
  labels:
    {{- include "elmis-kafka-sender-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "elmis-kafka-sender-chart.selectorLabels" . | nindent 4 }}
